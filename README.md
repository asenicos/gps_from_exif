# gps_from_exif — Retrieving GPS data from EXIF

A small Python 3 script, which retrieving GPS data from EXIF of folder and subfolders with JPG images. On the output is file with full file names and coordinates and HTML page, showing shooting location on the globe.
